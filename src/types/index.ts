export interface ExpertiseType {
    name: string
    currently_use: boolean
}

export interface ProjectType {
    name: string
    type: string
    url: string
    image: string
    on_progress: boolean
}
